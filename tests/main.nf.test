nextflow_pipeline {
    
    name "Test Workflow main.nf"
    script "main.nf"
    
    test("Should run successfully with correct number of processes") {

	when {
	}

        then {
            assert workflow.success
            assert workflow.trace.tasks().size() == 50
        }

    }
}
