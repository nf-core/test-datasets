# Dummy Data for Validation Testing

this dir holds dummy data (empty db dirs and empty files, some as `.tar.gz`) to facilitate stubbed runs like input validation testing without fetching reference data for each test run.
